# 네트워크 접속 설정 가이드

## 같은 네트워크에서 다른 컴퓨터 접속하기

### 1. 서버 컴퓨터 (호스트) 설정

**서버는 이미 모든 네트워크 인터페이스에서 수신하도록 설정되어 있습니다.**

서버를 실행하면:
```bash
cd server
npm run dev
```

서버가 `192.168.55.33:3001`에서 수신하므로 같은 네트워크의 모든 컴퓨터에서 접속 가능합니다.

### 2. 서버 컴퓨터의 IP 주소 확인

**Windows:**
```bash
ipconfig
```
IPv4 주소를 찾으세요 (예: `192.168.55.33`)

**Mac/Linux:**
```bash
ifconfig
# 또는
ip addr
```

### 3. 다른 컴퓨터에서 접속

**방법 1: 환경 변수 사용 (권장)**

다른 컴퓨터에서 `.env.local` 파일을 만들고:
```
NEXT_PUBLIC_SOCKET_URL=http://192.168.55.33:3001
```
(192.168.55.33을 서버 컴퓨터의 실제 IP로 변경)

**방법 2: 브라우저에서 직접 접속**

다른 컴퓨터의 브라우저에서:
```
http://192.168.55.33:3000
```
(192.168.55.33을 서버 컴퓨터의 실제 IP로 변경)

### 4. 방화벽 설정 (필요한 경우)

**Windows 방화벽:**
1. Windows 보안 → 방화벽 및 네트워크 보호
2. 고급 설정
3. 인바운드 규칙 → 새 규칙
4. 포트 선택 → TCP → 특정 로컬 포트: 3000, 3001
5. 연결 허용 선택
6. 모든 프로필에 적용

**또는 PowerShell에서:**
```powershell
New-NetFirewallRule -DisplayName "LEXIO Server" -Direction Inbound -LocalPort 3000,3001 -Protocol TCP -Action Allow
```

### 5. 확인 방법

1. **서버 컴퓨터에서:**
   - 프론트엔드: `http://localhost:3000`
   - 서버: `http://localhost:3001`

2. **다른 컴퓨터에서:**
   - 프론트엔드: `http://[서버IP]:3000` (예: `http://192.168.55.33:3000`)
   - 서버는 직접 접속 불가 (Socket.io만 사용)

### 6. 문제 해결

**연결이 안 될 때:**
1. 서버와 클라이언트가 같은 Wi-Fi/네트워크에 있는지 확인
2. 방화벽이 포트를 차단하지 않는지 확인
3. 서버 IP 주소가 올바른지 확인
4. 서버가 실행 중인지 확인

**IP 주소가 변경되면:**
- 서버 컴퓨터의 IP가 변경되면 다른 컴퓨터의 `.env.local`도 업데이트해야 합니다
- 또는 고정 IP를 사용하거나 DHCP 예약을 설정하세요

